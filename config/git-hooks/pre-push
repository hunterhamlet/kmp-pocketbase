#!/bin/sh
set -e

echo "Running pre-push checks..."
./gradlew ktlintCheck --daemon -q
echo "  ktlint OK"
./gradlew detekt --daemon -q
echo "  detekt OK"
./gradlew :app:shared:koverVerify --daemon -q
echo "  coverage OK"
echo "All checks passed."
